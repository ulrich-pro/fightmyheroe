import React, { memo } from 'react';
import { Alert } from 'react-bootstrap';

const Footer = memo(() => (
  <Alert variant="dark">Footer goes here! All right reserved - 2020</Alert>
));

export default Footer;
